# 사중력사목 온라인 V1

## 실행
```bash
npm install
npm start
```

브라우저에서 `http://localhost:3000` 으로 접속하세요.

## 파일 구조
- `server.js`: Express + Socket.IO 서버
- `public/index.html`: 클라이언트
- `package.json`: 의존성 및 실행 스크립트

## 배포
Render Web Service에서:
- Build Command: `npm install`
- Start Command: `npm start`
